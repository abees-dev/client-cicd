mutation addFriendRequest($data: AddFriendInput!) {
  addFriendMutation(data: $data) {
    code
    message
    addressee {
      id
      firstName
      lastName
      avatar
      createdAt
      email
    }
  }
}
