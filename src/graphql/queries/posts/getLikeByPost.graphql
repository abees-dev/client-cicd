query GetLikeByPost($likeInput: PostLikeQueryInput!) {
  getLikeByPost(likeInput: $likeInput) {
    code
    message
    likes {
      id
      type
      createdAt
      updatedAt
    }
    currentLike {
      like
      type
    }
    totalLike
  }
}
