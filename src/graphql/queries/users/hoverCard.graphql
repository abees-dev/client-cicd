query HoverCard($userId: String!) {
  hoverCard(userId: $userId) {
    isFriend
    user {
      id
      avatar
      firstName
      lastName
      createdAt
      profile {
        dayOfBirth
        gender
        liveAt
        phoneNumber
        province
        district
        ward
      }
    }
  }
}
